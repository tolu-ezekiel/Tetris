# TETRIS GAME

A node.js implementation of a text-mode version of the Tetris game

## Running
    node index.js

## Controls:
    a (return) -- move piece left
    d (return) -- move piece right
    w (return) -- rotate piece counter clockwise
    s (return) -- rotate piece clockwise
